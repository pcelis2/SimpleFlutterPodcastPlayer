import 'package:flutter/material.dart';

//colors
const Color MAIN_ICON_COLOR = Colors.blueAccent;
//elevation
const double kAppBarElevation = 10;
const double kBrandingElevation = 10;
const TextStyle whiteText = TextStyle(color: Colors.white);
const Shadow kShadowOrange =
    Shadow(blurRadius: 10.0, color: Colors.orange, offset: Offset(5.0, 5.0));
const Shadow kShadowBlue = Shadow(
    blurRadius: 10.0, color: Colors.blueAccent, offset: Offset(5.0, 5.0));
const Shadow kShadowGrey =
    Shadow(blurRadius: 10.0, color: Colors.grey, offset: Offset(5.0, 5.0));
const Shadow kShadowBlack =
    Shadow(blurRadius: 10.0, color: Colors.black, offset: Offset(5.0, 5.0));
const TextStyle kHeaderStyling = TextStyle(
  color: Colors.white,
  fontSize: 40,
  fontWeight: FontWeight.bold,
  fontFamily: 'Pacifico',
  shadows: [kShadowOrange],
);
